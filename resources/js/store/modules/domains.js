
// state
export const state = {
  data: null,
};

// mutations
export const mutations = {
  //
};

// actions
export const actions = {
  //
};

// getters
export const getters = {
  domains(state) {
    return state;
  },
};
